type Operator = {
    id: string
    firstName: string
    lastName: string
    name: string
    fatherName: string
    nationalIdNumber: string
    phoneNumber: string
    telephoneNumber: string
    address: string
}

export default Operator
