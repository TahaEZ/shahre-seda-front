import { ReactNode } from 'react'

export type DebtorCustomersColumn = {
    debt: ReactNode
    customer: string
    date: string
    totalPrice: ReactNode
    paid: ReactNode
    invoice: ReactNode
    id: string
}
